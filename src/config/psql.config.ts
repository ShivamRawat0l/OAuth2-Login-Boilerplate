const config = {
  host: 'localhost',
  user: 'postgres',
  password: 'root',
  port: 5432,
  max: 20,
  database: 'testing',
  idleTimeoutMillis: 30000,
  connectionTimeoutMillis: 2000,
}

export default config; 
